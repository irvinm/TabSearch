{
  "name": "tab_search",
  "version": "0.8.0.4",
  "private": true,
  "description": "Search tabs and hide non-matching ones.",
  "scripts": {
    "build": "npx web-ext build --source-dir src --overwrite-dest",
    "sign": "npx web-ext sign --source-dir src --channel unlisted",
    "test": "node --test"
  }
}
